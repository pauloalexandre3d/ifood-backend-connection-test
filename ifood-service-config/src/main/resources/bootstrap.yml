---
spring:
  application:
    name: ifood-service-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pauloalexandre3d/ifood-backend-connection-test
          searchPaths: ConfigData
server:
  port: 8001