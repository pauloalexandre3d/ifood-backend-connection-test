---
spring:
  cloud:
    config:
      name: ifood-service-gateway
      discovery:
        serviceId: ifood-service-config
        enabled: true

 eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/